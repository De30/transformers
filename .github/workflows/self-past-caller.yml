name: Self-hosted runner (past-ci-caller)

on:
  push:
    branches:
      - run_past_ci_pt_1.6

jobs:
  run_past_ci_pytorch_1-6:
    name: PyTorch 1.6
    if: always()
    uses: ./.github/workflows/self-past.yml
    with:
      framework: pytorch
      version: "1.6"
    secrets: inherit
